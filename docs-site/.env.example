# Docs site environment variables
# Copy this file to .env and edit values for local development.

# The base URL where docs are served. In production we serve under /docs/.
# For local dev to mirror production, set to /docs
DOCS_BASE_URL=/docs

# Optional: container port used by `docusaurus serve` when serving a built build
# PORT=3002
